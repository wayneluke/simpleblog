{% extends 'layout.html.twig' %}

{% block title %}{{ page.title }} · My Blog{% endblock %}

{% block body %}
  <article class="page">
    <h1>{{ page.title }}</h1>
    {{ page.html|raw }}
  </article>
{% endblock %}